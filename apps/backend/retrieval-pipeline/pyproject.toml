[project]
name = "retrieval-pipeline"
version = "0.1.0"
description = "Local retrieval indexing and query utilities for LAS-system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sentence-transformers>=5.2.3",
]
